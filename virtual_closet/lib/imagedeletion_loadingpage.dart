import 'package:flutter/material.dart';

class LoadingImageDeletion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(body: Center(child: Text('loading...')));
  }
}
